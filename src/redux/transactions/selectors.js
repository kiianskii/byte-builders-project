export const selectSummary = (state) => state.transactions.summary;
export const categories = (state) => state.transactions.categories;

export const transactionForEdit = (state) =>
  state.transactions.transactionForEdit;
